# 核心（轻量、秒装）——行情 + 研报即刻可用
fastapi>=0.110
uvicorn[standard]>=0.29
requests

# 全栈数据源（较重）——一致预期 / 新闻 / 公告 / K线 / 财务
# 未安装时对应端点会返回 501 + 安装提示，不影响行情/研报
akshare>=1.10
mootdx>=0.10
pandas
openpyxl  # SZSE ETF share xlsx
polars
pyarrow
duckdb  # 回测行情: 分区 parquet, 内存查, 不建 .db

# 期货 CTP 持仓只读查询（可选）——「我的持仓 → 期货账户」
# 未安装时 /api/portfolio/ctp 返回配置提示，不影响其余功能
# openctp-ctp

# 研究桌可选源（未装时对应端点 501 + 安装提示）
# baostock          # A 股日 K 兜底
# pykrx             # 韩股日 K
# ccxt              # 加密所统一适配（OKX/Binance 公开 REST 已内置, 不必装）
